# HELK nxlog clean up event before parsing message
# HELK build Stage: Alpha
# Author: Roberto Rodriguez (@Cyb3rWard0g)
# License: GPL-3.0

filter {
  if [type] == "nxlog-mordor" {
    mutate {
      rename => { "ProcessID" => "event_ProcessID" }
      add_field => { "z_logstash_pipeline" => "0002" }
    }
  }
}