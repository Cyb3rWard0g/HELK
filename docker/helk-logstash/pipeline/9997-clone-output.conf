output {
  if [type] == "clone" {
    elasticsearch {
      hosts => ["helk-elasticsearch:9200"]
      index => "clone-%{+YYYY.ww}"
      document_id => "%{[@metadata][log_hash]}"
      user => 'elastic'
      #password => 'elasticpassword'
    }
  }
}