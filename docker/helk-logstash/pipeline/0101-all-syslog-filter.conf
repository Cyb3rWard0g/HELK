# HELK syslog filter
# HELK build Stage: Alpha
# Author: Nate Guagenti (@neu5ron)
# License: GPL-3.0

filter {

  if [etl_input_application_protocol] == "syslog" {
    mutate {
      rename => { "port" => "etl_input_src_port" }
      add_field => { "etl_pipeline" => "syslog-rename_port_field" }
    }
  }

}