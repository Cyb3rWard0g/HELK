# HELK All filter conf file
# HELK build Stage: Alpha
# Author: Roberto Rodriguez (@Cyb3rWard0g), Nate Guagenti (@neu5ron)
# License: GPL-3.0

filter {
  if [message] {
    mutate {
      add_field => {
          "z_original_timestamp" => "%{@timestamp}"
          "event_etl_pipeline" => "all-filter-0098"
      }
      copy => {
          "message" => "event_original_message"
          "type" => "z_logstash_type"
      }
    }
    ruby {
      code => "event.set('event_etl_processed_timestamp', Time.now());"
      add_field => { "event_etl_pipeline" => "all-add_processed_timestamp" }
    }
  }
}